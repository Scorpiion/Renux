Todo list for Renux:

* Fix user permissions during build, now some files get owned by root
* Fix so the user only have to type it's password once, now sometimes it you have to do it again if sudo have timed out


